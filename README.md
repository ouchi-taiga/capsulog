# カプセログ

カプセルトイの発売カレンダーと、撮影した戦利品を飾るコレクション棚、ダブりの交換マッチング。

リポジトリ名・パッケージ名は `capsulog`。

## ドキュメント

| ファイル | 内容 |
|---|---|
| [docs/01-research.md](./docs/01-research.md) | 事前調査レポート（市場規模・競合・技術的実現可能性・課金の成立性） |
| [docs/02-requirements.md](./docs/02-requirements.md) | 要件定義（何を作るか） |
| [docs/03-design.md](./docs/03-design.md) | 設計書（どう作るか） |
| [docs/04-checklist.html](./docs/04-checklist.html) | 全体計画チェックリスト（進捗管理はここで行う。ブラウザで開く） |

## 3行サマリ

- 市場は6年で5倍（385億 → 1,960億円）と成長中。母数は十分
- **入荷情報・在庫検索は公式3社に取られており、そこでは戦わない**
- **写真で飾るコレクション棚＋ダブり交換マッチング**を軸にする。公式画像は使わず、写真はユーザー撮影のみ

## フェーズ

| フェーズ | 期間目安 | 内容 | 課金 |
|---|---|---|---|
| 1 | 1〜3ヶ月 | 発売カレンダー＋コレクション棚 | 無料 |
| 2 | 4〜5ヶ月目 | ダブり交換マッチング | 無料 |
| 3 | 6ヶ月目〜 | 棚のプレミアム機能・マッチングの高度化 | 月額200〜300円 |

## 対象メーカー

| メーカー | 件数 | 取得元 | ラインナップ名 |
|---|---|---|---|
| 奇譚クラブ | 955 | サイトマップ + 詳細ページ | 取れる |
| ターリン | 616 | API | 取れない |
| タカラトミーアーツ | 約47/月 | 月別カレンダー + 詳細ページ | 取れない |
| Qualia | 数百 | HTML + 詳細ページ | 取れない |

**バンダイは収集しない。** 利用条件が自動取得を禁じているため。運営の手入力とユーザーのフリーテキスト登録で扱う。

**ラインナップ名が取れるのは奇譚クラブのみ。** コンプ率表示は当面この1社の商品でしか成立しない。

## 構成

| ディレクトリ | 役割 | 言語 |
|---|---|---|
| [batch/](./batch/) | データ収集。GitHub Actions で日次実行 | Python |
| [web/](./web/) | 画面と API。Cloudflare Workers | SvelteKit |
| [db/](./db/) | D1 のスキーマとマイグレーション | SQL |
| [probe/](./probe/) | メーカーからデータが取れるかの検証 | Python |

各ディレクトリの README に、そこでの決まりを書いている。

収集バッチを Workers ではなく GitHub Actions で動かすのは、奇譚クラブの953件を1秒間隔で回すと約16分かかり、Workers の CPU 時間に収まらないため。

インフラは全て無料枠で運用する。

## 開発環境

Dev Container で開発する。ホスト側に Node や Python を入れる必要はない。

VS Code で「Reopen in Container」を実行すると、必要なものが揃った状態で起動する。

### バージョン

**固定せず最新に追従する。** 個人開発で長く放置すると追従コストが上がるため、常に最新を使う。

| | 指定 | 供給元 |
|---|---|---|
| Node | LTS | devcontainer feature |
| pnpm | 最新 | corepack が用意する |
| Python | 最新安定版 | uv が管理する |
| uv | 最新 | 公式イメージからコピー |
| Claude Code | 最新 | devcontainer feature |

依存の更新で壊れたときは、バージョンを固定するのではなく原因を直す。固定するのは直せない理由があるときだけ。

### 認証

コンテナ内で一度だけ実行する。

```bash
npx wrangler login   # Cloudflare
claude               # Claude Code。初回起動時に認証する
```

**認証情報は volume に保存されるため、コンテナを作り直しても消えない。** ホスト側の認証情報をコンテナに渡す方法は取らない。コンテナ内のコードがホストの資格情報に触れられる状態を避けるため。

| 用途 | volume | マウント先 |
|---|---|---|
| Cloudflare | `capsulog-wrangler` | `~/.config/.wrangler` |
| Claude Code | `capsulog-claude` | `~/.claude` |

Claude Code は設定ファイルがホーム直下にも散らばるため、`CLAUDE_CONFIG_DIR` で保存先を `~/.claude` にまとめている。

## ドキュメントの書き方

- 内容はなるべくシンプルに書く
- 備考は書きすぎない。人の目で読みやすい分量に留める
- 注意書きを点在させない
- 他ドキュメントへの参照を書かない
- 最初からそう決まっていたように書く。後から更新したような書き方にしない
- 作業が完了したら**必ずチェックリストを更新すること**
