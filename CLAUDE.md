# CLAUDE.md

カプセログ。メーカー横断のカプセルトイ発売カレンダーと、ラインナップ単位のコレクション管理サービス。

## 構成

| ディレクトリ | 役割 | 言語 | README |
|---|---|---|---|
| `batch/` | メーカーからのデータ収集。日次実行 | Python | [batch/README.md](./batch/README.md) |
| `api/` | APIエンドポイント。Cloudflare Workers 上で動く | Rust / WASM | [api/README.md](./api/README.md) |
| `web/` | フロントエンド | SvelteKit | [web/README.md](./web/README.md) |
| `db/` | D1 のスキーマとマイグレーション | SQL | [db/README.md](./db/README.md) |
| `probe/` | 各メーカーからデータが取れるかの検証 | Python | [probe/README.md](./probe/README.md) |
| `docs/` | 調査・要件定義・設計・全体計画 | — | — |

**各ディレクトリで作業するときは、そのディレクトリの README を読むこと。**

## ドキュメント

| ファイル | 内容 |
|---|---|
| [docs/01-research.md](./docs/01-research.md) | 事前調査 |
| [docs/02-requirements.md](./docs/02-requirements.md) | 要件定義（何を作るか） |
| [docs/03-design.md](./docs/03-design.md) | 設計書（どう作るか） |
| [docs/04-checklist.html](./docs/04-checklist.html) | 全体計画。進捗はここで管理する |

## 守ること

### 呼び方

**「ガチャ」「ガチャガチャ」「ガシャポン」を使わない。** メーカーの登録商標のため。コード・コメント・画面上の文言・コミットメッセージのいずれでも使わない。

**「カプセルトイ」を使う。** 一般名詞であり、メーカー自身もそう使い分けている。

### スコープ

やらないと決めたものがある。要件定義の「やらないこと」を必ず確認する。特に**入荷情報と在庫検索は作らない。**

### 収集

- 収集するのは事実情報のみ。**商品説明文と画像は保存も表示もしない**
- リクエストは1秒以上あける
- 出典として公式ページへのリンクを必ず持つ

### 発売日

**日単位のカラムを作らない。** メーカーが月単位までしか公開していないため。発売月は NULL を許容する。

### 書き方

- 内容はなるべくシンプルに書く
- 備考は書きすぎない。人の目で読みやすい分量に留める
- 注意書きを点在させない
- 他ドキュメントへの参照を書かない
- 最初からそう決まっていたように書く。後から更新したような書き方にしない
- 作業が完了したら**必ずチェックリストを更新すること**

## 開発環境

Dev Container で開発する。ホスト側に Node や Rust を入れる必要はない。

バージョンは固定せず最新に追従する。Node は LTS、Rust と Python は最新安定版。

認証情報は volume に保存され、コンテナを作り直しても残る。**`~/.claude` と `~/.config/.wrangler` を消さないこと。**
