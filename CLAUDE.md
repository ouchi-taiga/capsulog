# CLAUDE.md

カプセログ。カプセルトイの発売カレンダーと、撮影した戦利品を飾るコレクション棚、ダブりの交換マッチング。

## 構成

| ディレクトリ | 役割 | 言語 | README |
|---|---|---|---|
| `batch/` | メーカーからのデータ収集。日次実行 | Python | [batch/README.md](./batch/README.md) |
| `web/` | 画面と API。Cloudflare Workers 上で動く | SvelteKit | [web/README.md](./web/README.md) |
| `db/` | D1 のスキーマとマイグレーション | SQL | [db/README.md](./db/README.md) |
| `probe/` | 各メーカーからデータが取れるかの検証 | Python | [probe/README.md](./probe/README.md) |
| `docs/` | 調査・要件定義・設計・全体計画 | — | — |

**各ディレクトリで作業するときは、そのディレクトリの README を読むこと。**

## ドキュメント

| ファイル | 内容 |
|---|---|
| [docs/01-research.md](./docs/01-research.md) | 事前調査 |
| [docs/02-requirements.md](./docs/02-requirements.md) | 要件定義（何を作るか） |
| [docs/03-design.md](./docs/03-design.md) | 設計書（どう作るか） |
| [docs/04-checklist.html](./docs/04-checklist.html) | 全体計画。進捗はここで管理する |

## 守ること

### 呼び方

**「ガチャ」「ガチャガチャ」「ガシャポン」を使わない。** メーカーの登録商標のため。コード・コメント・画面上の文言・コミットメッセージのいずれでも使わない。

**「カプセルトイ」を使う。** 一般名詞であり、メーカー自身もそう使い分けている。

### スコープ

やらないと決めたものがある。要件定義の「やらないこと」を必ず確認する。特に**入荷情報と在庫検索は作らない。**

### 設計

後から変えにくいものほど、先に幅を持たせる。特に DB のスキーマ。

| 性質 | 意味 | 具体例 |
|---|---|---|
| 拡張性 | 対象が増えても列を増やさずに済む | 発売時期は粒度と値を分けて持つ。旬も週も同じ列に入る |
| 冪等性 | 何度実行しても結果が同じ | 収集は UNIQUE 制約で upsert する。再実行で重複しない |
| 堅牢性 | 一部が壊れても全体を巻き込まない | 1社の収集が失敗しても他社は取り込む |
| 追跡可能性 | 元の値に遡れる | 正規化した値とは別に、生値を残す |

**取れないものを埋めない。** NULL を許容し、0 や空文字で代用しない。

### 収集

- 収集するのは事実情報のみ。**商品説明文と画像は保存も表示もしない**
- **バンダイからは取らない。** 利用条件が自動取得を禁じている
- リクエストは1秒以上あける
- 出典として公式ページへのリンクを必ず持つ

### 画像

**メーカーの公式画像は使わない。** ホットリンクも含めて。
画像はユーザーが撮影した現物写真のみを扱う。

### 発売日

**日単位のカラムを作らない。** メーカーが月単位までしか公開していないため。発売月は NULL を許容する。

### 書き方

- 内容はなるべくシンプルに書く
- 備考は書きすぎない。人の目で読みやすい分量に留める
- 注意書きを点在させない
- 他ドキュメントへの参照を書かない
- 最初からそう決まっていたように書く。後から更新したような書き方にしない
- 作業が完了したら**必ずチェックリストを更新すること**

### コメント

- 冗長に書かない
- 一行を長くしない
- 句読点で改行する。句点を優先する。無い場合は意味の切れ目で折る
- 絵文字を使わない

### コミットメッセージ

**コミットは人がする。Claude は実行しない。** メッセージだけを出力して渡す。

**区切りがついたらその都度メッセージを出す。** まとめて渡さない。1コミットは小さく保つ。

Conventional Commits に従い、説明は日本語で書く。なるべくシンプルに。

小さい修正は件名だけでよい。

```text
fix: probe の User-Agent からメーカーの商標を外す
```

修正量があるときは、件名に続けて本文を箇条書きで書く。

```text
feat: 2社分の収集処理を追加

- 奇譚クラブ・ターリンの取得を実装
- 取り込み前に件数と値を検証し、異常なら該当メーカーを中止する
- content_hash で差分を見て、変わったものだけ更新する
```

| type | 使いどころ |
|---|---|
| `feat` | 機能の追加 |
| `fix` | 不具合の修正 |
| `docs` | ドキュメントのみの変更 |
| `refactor` | 挙動を変えない書き換え |
| `chore` | 設定・依存・その他 |

## 開発環境

Dev Container で開発する。ホスト側に Node や Python を入れる必要はない。

バージョンは固定せず最新に追従する。Node は LTS、Python は最新安定版。

認証情報は volume に保存され、コンテナを作り直しても残る。**`~/.claude` と `~/.config/.wrangler` を消さないこと。**
